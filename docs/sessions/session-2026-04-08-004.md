# Session Handoff — 2026-04-08-004

**Phase:** 3 — Archivist System + Curator + Hybrid RAG (complete)
**Date:** 2026-04-08

## Completed This Session

25 commits on `phase/3-archivist-curator-rag` (bb433bb..ae608f0). All 19 Phase 3 deliverables complete (14 original + 5 added during session).

### Core Archivist System (bb433bb, c6e93c3, 2ece5d1, 576c754)

- ArchivistDB (SQLite + FTS5 + sqlite-vec), Archivist service, embedding generation with chunking, hybrid retrieval with RRF, FTS query sanitization.
- 76 tests.

### Multi-Pass Retrieval (e2972e8, 4089170)

- 4-pass pipeline in `rag/multi_pass.py` with short-circuit for small KBs. Graceful degradation without LLM.
- 14 tests.

### Curator Agent (195bdad, 4089170, da57809)

- Cross-referencing, singleton enforcement, proposal generation, retroactive cross-referencing, toolkit (backlink scanner, graph analyzer with exact stem matching).
- 28 tests.

### LLM Response Cache (b229f48, 4089170)

- SQLite-backed, SHA-256 keyed, TTL, flush per agent/expedition, content-length-based eviction, integrated into LLMClient.
- 12 tests.

### Image Ingestion (313e5a3, da57809, 162158a, a3658a3, e2a8165, 69d420b)

- `convert_image()` with proper multimodal content blocks format, `is_image_file()` detection, auto-resize via Pillow for images >5MB, improved transcription prompt.
- Scout CLI wired to workspace config for provider routing.
- Pillow added as dependency (required by litellm for Ollama image processing).
- 13 tests.

### Multi-Source Compilation (62deae7)

- `compile_multi()` on CompilerAgent — accepts multiple source paths, concatenates in order, tracks combined hash in manifest, lists all sources in frontmatter.
- CLI `--source` repeatable for multi-source missions.
- 8 tests.

### Batch Ingestion (62deae7)

- `scout ingest` accepts multiple file arguments.
- Part of existing test suite.

### CLI Commands (7d49722, 313e5a3, da57809)

- `assistonauts status` — KB overview with article counts, word count, stale detection.
- `assistonauts index` — FTS + optional embeddings, `--reindex` flag.
- `assistonauts curate --proposals` — structural analysis (orphans, density).
- 12 tests.

### Evaluator Fix Rounds

- Round 1 (4089170): Multi-pass passes 3-4, LLM cache integration, singleton, proposals, FakeEmbeddingClient consolidation, embedding_hash, numpy removal, max_size_mb.
- Round 2 (da57809): Multimodal format, cache eviction, index embeddings, graph exact matching, type hints.

## In Progress

**Manual build pipeline test** — `docs/manual/task-2026-04-08-002.md`. User has ingested images with Haiku 4.5, ready to compile. Bugs found and fixed during testing: Pillow dependency, Scout config wiring, image resize for API limits, Gemma 4 vision limitations.

## Blocked

**Branch push + PR** — `docs/manual/task-2026-04-08-001.md`. Sandbox firewall blocks git push. Must be done outside sandbox before Phase 4 can start.

## Issues & Technical Debt

1. **FTS5 boolean operators** (important) — NOT/AND/OR not escaped. `database.py:180`.
2. **`_parse_frontmatter` simple parser** (important) — Lists/nested YAML won't parse. `service.py:23`.
3. **Curator title extraction** (important) — Uses `# ` heading not frontmatter. `curator.py:137`.
4. **Multi-pass Pass 3 truncates at 2000 chars** (important) — `multi_pass.py:271`.
5. **`curate` without `--proposals` is a no-op** (important) — Needs `--model` flag.
6. **No contract tests for Phase 3** (important) — All unit tests, no recorded fixtures.
7. **`status.py` variable scoping** (minor) — `indexed_articles`/`archivist` in separate `if` blocks.
8. **`_should_short_circuit` redundant check** (minor) — Dead code line 133.
9. **LLM cache `flush` ignores expedition when agent provided** (minor).
10. **DB connections not explicitly closed in CLI** (minor).
11. **`_ingest_one` uses `object` types** (minor) — `scout.py` helper function.

## Evaluator Results

- **First pass:** 2.85/5.0 — FAIL. All 4 critical + 8 important fixed.
- **Second pass:** 3.00/5.0 — PASS WITH ISSUES. 3 critical fixed, remaining issues documented above.
- Post-evaluator work (image resize, multi-source, batch ingest, config wiring) was not re-evaluated but is tested and lint-clean.

## Test State

- Total tests: 369
- Passing: 369
- Failing: 0
- Skipped: 0

## Build State

- Build: clean
- Lint: clean (`ruff check src/`)
- Format: clean (`ruff format --check src/`)

## Next Steps

1. **Complete manual pipeline test** — finish compiling, indexing, and curating the "There's Treasure Inside" book. Use multi-source compilation for the Introduction (5 page spreads). — already in progress
2. **Push branch + create PR** — `docs/manual/task-2026-04-08-001.md` — required before Phase 4
3. **Phase 4: Explorer agent** — query flow via multi-pass retrieval, answer synthesis with citations — large
4. **Phase 4: Interactive REPL** — Click-based CLI for human Q&A — medium

## Session Notes

- **Gemma 4 cannot handle vision/transcription** — both e2b and 31b produce garbled output from book page screenshots. Claude Haiku 4.5 works well. Per-role provider routing allows mixing models (Haiku for scout vision, Ollama for compilation).
- **litellm requires Pillow** for Ollama image processing — not documented by litellm, discovered during testing. Added to pyproject.toml.
- **Anthropic API 5MB image limit** — book page spreads can be 10-15MB. Auto-resize with progressive downscaling via Pillow handles this transparently.
- **Scout CLI was not reading workspace config** — fixed in a3658a3. The `_create_llm_client` pattern from mission.py was missing from scout.py.
- **Multi-source compilation uses combined SHA-256 hash** — hash of all individual source hashes joined by `|`. Any single source changing triggers recompilation.
- **FakeEmbeddingClient** consolidated in `tests/helpers.py` alongside FakeLLMClient.
- **CLAUDE.md** already updated this session: Phase advanced to 4, bootstrapping removed, test helpers documented.
