# Session Handoff — 2026-04-08-005

**Phase:** 2/3 — Cross-phase work (vocabulary refactor + Compiler plan mode)
**Date:** 2026-04-08

## Completed This Session

This session completed the final two Phase 2 deliverables and several Phase 3 improvements discovered during real-world testing. 35 commits on `phase/3-archivist-curator-rag` (bb433bb..6fd825a).

### Phase 3 Core (commits bb433bb..576c754, 4089170, da57809)

- Archivist system, embeddings, FTS5, hybrid retrieval with RRF, multi-pass retrieval, Curator agent, LLM response cache, retroactive cross-referencing. All evaluated and fixed across two evaluator rounds. Details in session-2026-04-08-003 and session-2026-04-08-004.

### Phase 3 Additions from Real-World Testing (commits 162158a..69d420b, 313e5a3)

- **Image ingestion** — `convert_image()` with proper multimodal content blocks, auto-resize via Pillow for >5MB images, improved transcription prompt.
- **CLI commands** — `assistonauts status`, `assistonauts index`, `assistonauts curate`.
- **Batch ingestion** — `scout ingest` accepts multiple files.
- **Multi-source compilation** — `compile_multi()`, `--source` repeatable in CLI.
- Bugs fixed: Pillow dependency, Scout config wiring, Gemma 4 vision limitations (switched to Haiku), default config to Anthropic/Haiku.

### Task/Mission Vocabulary Refactor (commit ef9f591)

- Renamed "mission" → "task" across 19 files: `missions/` → `tasks/`, `MissionRunner` → `TaskRunner`, `Mission` → `Task`, `run_mission()` → `run_task()`, CLI `mission run` → `task run`.
- Pure mechanical rename, 369 tests pass unchanged.
- Added Glossary to REQUIREMENTS.md: Task (atomic), Mission (multi-step), Triage (editorial).

### Compiler Plan Mode (commit 1b26195, 6fd825a)

- `compiler.plan()` reads raw sources, proposes compilation plan via LLM with YAML output.
- `PlannedArticle` and `CompilationPlan` data models.
- `_parse_plan_yaml()` with defensive parsing and fallback (one article per source on parse failure).
- CLI: `assistonauts plan [-w workspace] [--execute]`.
- Evaluator fix: `articles: null` crash guard, structured logging for plan LLM calls, stale doc references.
- 9 tests.

## In Progress

**Manual build pipeline test** — `docs/manual/task-2026-04-08-002.md`. User ingested images with Haiku 4.5. Ready to test `assistonauts plan --execute` with real content.

## Blocked

**Branch push + PR** — `docs/manual/task-2026-04-08-001.md`. Sandbox firewall blocks git push.

## Issues & Technical Debt

From evaluator (important/minor, non-blocking):

1. **Plan CLI test coverage minimal** — only `--help` test, no workspace/source/execute tests. `test_compiler_plan.py:169`.
2. **No test for unmatched source names in plan response** — silently dropped.
3. **No test for code-fence stripping in `_parse_plan_yaml`** — untested path.
4. **No test for source content truncation at 2000 chars** in plan prompt.
5. **Plan CLI hardcodes `raw/articles/*.md`** — subdirectories and non-md files silently ignored.

Carried from prior evaluations: 6. **FTS5 boolean operators not escaped** — `database.py:180`. 7. **`_parse_frontmatter` simple parser** — `service.py:23`. 8. **Curator title extraction uses heading not frontmatter** — `curator.py:137`. 9. **No contract tests for Phase 3 agents**.

## Evaluator Results

- **Refactor + Plan Mode:** 3.45/5.0 — PASS WITH ISSUES. 1 critical (articles:null crash — fixed), 5 important (4 fixed, 1 test coverage gap noted).
- **Phase 3 Core (earlier):** 3.00/5.0 — PASS WITH ISSUES after fixes.

## Test State

- Total tests: 378
- Passing: 378
- Failing: 0
- Skipped: 0

## Build State

- Build: clean
- Lint: clean
- Format: clean

## Next Steps

1. **Complete manual pipeline test** — run `assistonauts plan --execute -w /tmp/test-kb` with the "There's Treasure Inside" book screenshots. This is the first real test of plan mode.
2. **Push branch + create PR** — `docs/manual/task-2026-04-08-001.md`.
3. **Phase 4: Explorer agent** — query flow via multi-pass retrieval, answer synthesis with citations — large.
4. **Phase 4: Interactive REPL** — Click-based CLI for human Q&A — medium.

## Session Notes

- **Gemma 4 can't do vision transcription** — both e2b and 31b produce garbled output. Haiku 4.5 works. Default config switched to Anthropic/Haiku for all roles.
- **Anthropic API has 5MB image limit** — auto-resize with progressive downscaling via Pillow handles this.
- **Task vs Mission vocabulary** is now established: tasks are atomic (Phase 2), missions are multi-step objectives (Phase 5). The spec uses "mission" correctly in the multi-step sense.
- **Compiler plan mode** is the editorial triage layer. The Captain (Phase 5) orchestrates when plans execute but doesn't make article type/grouping decisions.
- **The manual pipeline** is now: `scout ingest` → `plan [--execute]` → `index` → `curate --proposals` → `status`.
