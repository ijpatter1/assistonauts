# Manual Task — Push Phase 3 Branch and Create PR

**Created:** 2026-04-08, session-2026-04-08-003
**Phase:** 3
**Status:** pending
**Priority:** high
**Blocks:** Phase 4 start (Explorer + Interactive Mode builds on merged Phase 3 code)

## Context

Phase 3 (Archivist System + Curator + Hybrid RAG) was fully implemented inside the sandbox across 10 commits on the `phase/3-archivist-curator-rag` branch. All 14 deliverables are complete.

**Current state:**

- 340 tests passing, 0 failing
- Lint clean (`ruff check` + `ruff format`)
- 10 commits on `phase/3-archivist-curator-rag`
- The sandbox firewall blocks `git push` to GitHub

**Key files added/modified this session:**

- `src/assistonauts/archivist/` — 4 modules (database, service, embeddings, retrieval)
- `src/assistonauts/rag/multi_pass.py` — multi-pass retrieval system
- `src/assistonauts/agents/curator.py` — Curator agent
- `src/assistonauts/tools/curator.py` — Curator toolkit
- `src/assistonauts/cache/llm_cache.py` — LLM response cache
- `src/assistonauts/cli/status.py` — status command
- `src/assistonauts/llm/client.py` — LLM cache integration
- `tests/` — 12 new test files, 119 new tests (221 → 340 total)
- `pyproject.toml` — added sqlite-vec dependency
- `docs/PHASE_STATUS.md` — all 14 deliverables marked ✅

**Known issues (documented in handoff, non-blocking for merge):**

- Dual summary storage uses SQLite table instead of JSONL (intentional)
- `_parse_frontmatter` is a simple parser (doesn't handle lists/nested YAML)
- Graph analyzer uses substring matching for slug resolution
- No contract tests for Curator (unit tests only)
- FTS5 operator words (NOT, AND, OR) not escaped

## Steps

1. **Push the branch to GitHub**

   ```bash
   cd /path/to/assistonauts
   git push origin phase/3-archivist-curator-rag
   ```

2. **Verify tests pass locally (outside sandbox)**

   ```bash
   source .venv/bin/activate
   pip install -e ".[dev]"
   pip install sqlite-vec
   pytest -v
   ruff check src/ tests/
   ruff format --check src/ tests/
   ```

   - Expected result: 340 tests pass, lint clean, format clean

3. **Verify CLI works**

   ```bash
   assistonauts status --help
   assistonauts status -w /tmp/test-kb  # should error (no workspace)
   assistonauts init /tmp/test-kb
   assistonauts status -w /tmp/test-kb  # should show empty stats
   ```

4. **Create PR to merge into main**

   ```bash
   gh pr create \
     --base main \
     --head phase/3-archivist-curator-rag \
     --title "Phase 3: Archivist System + Curator + Hybrid RAG" \
     --body "## Summary

   Implements all 14 Phase 3 deliverables:
   - Archivist system (deterministic KB operating system, no LLM)
   - SQLite database with FTS5 + sqlite-vec for hybrid retrieval
   - Embedding generation with chunking and batching
   - Reciprocal Rank Fusion hybrid search
   - 4-pass multi-pass retrieval system with short-circuit mode
   - Curator agent with singleton enforcement and cross-referencing
   - Curator toolkit (backlink scanner, graph analyzer)
   - LLM response cache (SQLite, SHA-256 keyed, TTL, eviction)
   - Retroactive cross-referencing for Phase 1-2 articles
   - CLI: \`assistonauts status\` command

   ## Test plan
   - 340 tests passing, 0 failing
   - \`pytest && ruff check src/ tests/ && ruff format --check src/ tests/\`
   - Manual: \`assistonauts status -w /path/to/workspace\`

   🤖 Generated with [Claude Code](https://claude.com/claude-code)"
   ```

5. **Review and merge the PR**

## Verification

- [ ] `git log origin/phase/3-archivist-curator-rag --oneline -10` shows all 10 commits
- [ ] `pytest` passes with 340 tests outside the sandbox
- [ ] `assistonauts status -w /tmp/test-kb` shows knowledge base status table
- [ ] PR exists on GitHub
- [ ] After merge, `main` branch contains all Phase 3 code

## Report Back

When complete, update the **Status** field above to `done` and add:

**Completed:** YYYY-MM-DD
**Notes:** (test results, any issues encountered)
