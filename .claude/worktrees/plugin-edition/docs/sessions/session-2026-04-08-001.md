# Session Handoff — 2026-04-08-001

**Phase:** 2 → 3 (Phase 2 merged, Phase 3 ready to start)
**Date:** 2026-04-08

## Completed This Session

This was a minimal session focused on closing out Phase 2:

1. **Default Ollama model updated to `gemma4:e2b`** — Changed `src/assistonauts/storage/workspace.py:11` from `ollama/gemma4` to `ollama/gemma4:e2b`. The full `gemma4` (31B) was too large for testing; `e2b` is the smaller variant the user prefers for development.

2. **Manual task card marked complete** — `docs/manual/task-2026-04-07-002.md` updated: status → done, all 6 verification checkboxes checked, completion notes added with test results from the user's local machine.

3. **Phase 2 branch merged into main** — User pushed `phase/2-compiler-mission-runner` to origin, created PR #3, and merged into main (commit `747669b`).

## In Progress

Nothing in progress.

## Blocked

Nothing blocked.

## Issues & Technical Debt

Carried forward from evaluator — these should be addressed early in Phase 3:

1. **Manifest key semantics** (important) — Wiki path keys (`wiki/concept/foo.md`) store the hash of the _source_ file, not the wiki article. Phase 3's Archivist will consume the manifest and may expect wiki-path entries to hash wiki content. Needs documentation or refactoring. Location: `src/assistonauts/agents/compiler.py:135-138`.

2. **`MissionResult.agent_output: object`** (important) — Effectively untyped. CLI uses `hasattr` + `type: ignore` to access `.output_path`. Should be `Union[CompilationResult, ScoutResult, None]` or a Protocol. Location: `src/assistonauts/missions/runner.py:56`.

3. **Duplicate FakeLLMClient** (important) — 5 copies across test files. `tests/conftest.py` has the shared version but 4 other files define their own. Should consolidate to conftest.

4. **`_DEFAULT_MODEL = "gpt-3.5-turbo"` fallback** (minor) — In `src/assistonauts/config/resolver.py:7` and `src/assistonauts/llm/client.py:38`. Doesn't match workspace default (`ollama/gemma4:e2b`). Should align or document divergence.

5. **`git add -A` in mission commits** (important for Phase 5) — `src/assistonauts/missions/runner.py:203` stages all changes. Safe for single-mission execution but will cause cross-contamination with concurrent missions in Phase 5.

6. **Swallowed `CalledProcessError` in `_git_commit`** (minor) — `src/assistonauts/missions/runner.py:217-218` has `pass` with no logging. Git failures are invisible.

## Evaluator Results

- **Weighted score:** 4.15/5.0
- **Verdict:** PASS
- **Critical issues:** None
- **Important issues:** 5 (manifest semantics, agent_output typing, duplicate FakeLLMClient, git add scope, base Agent.run_mission typing)
- **Minor issues:** 4

## Test State

- Total tests: 219
- Passing: 219
- Failing: 0
- Skipped: 0

## Build State

- Build: clean (Python, no compile step)
- Lint: clean (`ruff check src/`)
- Format: clean (`ruff format --check src/`)

## Next Steps

### Phase 3 — Archivist System + Curator + Hybrid RAG

1. **Address manifest key semantics** — Clarify or refactor before Archivist builds on it. Small.
2. **Archivist system core** — Main `Archivist` class with `index()`, `search()`, `reindex_batch()`, `get_staleness()`, `get_downstream()`. Not an agent, no LLM. Medium.
3. **Embedding generation and storage** — Via litellm, chunking, batching, sqlite-vec storage. Medium.
4. **FTS indexing** — SQLite FTS5 insert/update/delete for keyword search. Medium.
5. **Hybrid retrieval** — Vector similarity + FTS keyword, reciprocal rank fusion reranking. Medium.
6. **Dual summary storage** — Retrieval + content summaries in `index/summaries.jsonl`. Small.
7. **Multi-pass retrieval system** — 4-pass system in `rag/multi_pass.py`. Large.
8. **Curator agent** — Cross-referencing, backlink scanning, graph analysis. Large.

## Session Notes

- User confirmed the end-to-end pipeline works on their local machine with Ollama: init → scout ingest → compiler mission run → article + summary output. See `docs/manual/task-2026-04-07-002-output.html` for full terminal output.
- The `Modelfile` in the repo root is untracked — appears to be a local Ollama model config file. Not committed.
- Phase 3 will need `sqlite-vec` and SQLite FTS5 — verify these dependencies are in `pyproject.toml` before starting.
- The user's local environment runs on macOS with a `.venv` virtualenv. The sandbox uses system Python with `--break-system-packages`.
