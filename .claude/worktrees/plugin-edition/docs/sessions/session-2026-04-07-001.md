# Session Handoff — 2026-04-07-001

**Phase:** 1 — Core Infrastructure + Scout
**Date:** 2026-04-07

## Completed This Session

### Spec Refinements (commit d46566d)

- Split Phase 1 into 1a (Core Infrastructure + Scout) and 1b (Compiler + Mission Runner), renumbered phases 2-6 → 3-7
- Added Testing Strategy section to spec (contract tests, recorded fixtures, integration tests)
- Moved wiki versioning from Open Questions to resolved design decision (git-tracked workspace, commit per mission)
- Refined mission retry to distinguish transient vs deterministic failures
- Added SQLite concurrency ceiling note to Agent Scaling section
- Added content summary quality gap note to Multi-Pass Retrieval section

### Documentation Scaffolding (commit 3a0fc8d)

- `docs/REQUIREMENTS.md` — 7 phases, 67 deliverables with validation criteria and dependency graph
- `docs/ARCHITECTURE.md` — full Phase 1 architecture (package structure, base agent class, LLM client, config system, manifest, Scout agent, workspace init, testing approach, dependencies), 6 phases stubbed
- `docs/PHASE_STATUS.md` — 67 deliverables tracked, all ⬜
- `CLAUDE.md` — configured for Python/pytest/ruff/uv/litellm stack
- `README.md` — replaced template with Assistonauts-specific project readme

### Project Scaffolding (commit a4355e6)

- `pyproject.toml` with hatchling build, Phase 1 deps, pytest + ruff dev deps, console script entry point
- `src/assistonauts/` package layout with `__init__.py`, `__main__.py`, `cli/main.py` (Click group), empty subpackage dirs for agents, tools, llm, config, cache, storage, models
- `tests/conftest.py` with shared fixtures (tmp_workspace)
- `tests/unit/test_smoke.py` with 3 smoke tests (version, CLI help, CLI version)

## In Progress

### Project scaffolding — dependency installation blocked

- **Current state:** All code files written and committed. Dependencies cannot be installed because the Docker sandbox firewall blocks PyPI (`pypi.org`, `files.pythonhosted.org`).
- **What remains:** `pip install -e ".[dev]"`, verify all 3 smoke tests pass, verify `ruff check` and `ruff format --check` are clean, verify `assistonauts --help` works.
- **Where to pick up:** Rebuild sandbox image with PyPI domains in firewall allowlist, then run install + tests.

## Blocked

### Sandbox firewall blocks PyPI

- **Blocker:** `sandbox/init-firewall.sh` does not include `pypi.org` or `files.pythonhosted.org` in the domain allowlist. All `pip install` attempts fail with `Network is unreachable`.
- **What's needed:** Add PyPI domains to `PROJECT_DOMAINS` in `sandbox/init-firewall.sh`, rebuild the sandbox image (`make build`).
- **Impact:** Blocks ALL implementation work — no dependencies, no tests, no linting, no CLI verification.
- **Note:** The user is rebuilding the sandbox image to fix this.

## Issues & Technical Debt

From evaluator (must fix next session):

1. **Python version inconsistency (critical)** — `pyproject.toml` says `>=3.11` but `CLAUDE.md`, `README.md`, and `docs/ARCHITECTURE.md` all say "3.12+". Reconcile to match the actual runtime (3.11 in sandbox). Severity: critical.
2. **Makefile `dev` target (important)** — Still says `npm run dev` from template. Remove or rewrite for Python CLI. File: `Makefile:117-118`. Severity: important.
3. **Architecture doc type hint (important)** — `dict[str, callable]` should be `dict[str, Callable[..., Any]]`. File: `docs/ARCHITECTURE.md` base agent class section. Severity: important.
4. **CLAUDE.md directory tree (important)** — Shows `archivist/`, `missions/`, `rag/` without "(Phase N)" annotation. File: `CLAUDE.md` Directory Structure section. Severity: important.
5. **conftest.py unused imports (minor)** — `os` and `tempfile` imported but unused, `clean_env` fixture unused. File: `tests/conftest.py`. Severity: minor.
6. **`__main__.py` missing guard (minor)** — Should use `if __name__ == "__main__":` guard. File: `src/assistonauts/__main__.py`. Severity: minor.

## Evaluator Results

- **Weighted score:** 2.30/5.0
- **Verdict:** FAIL
- **Rationale:** Tests written but never verified to pass (firewall blocked pip install). Python version inconsistency across 4 docs. No session handoff artifact at time of evaluation (being written now).
- **Note:** The FAIL is primarily due to the sandbox firewall blocking all dependency installation. The documentation work itself was rated positively. Once the firewall is fixed and tests verified green, the session's scaffolding work should be sound.

## Test State

- Total tests: 3 (written, unverified)
- Passing: unknown (dependencies not installed)
- Failing: unknown
- Skipped: 0
- Coverage: not configured yet

## Build State

- Build: cannot verify (hatchling not installed)
- Lint: cannot verify (ruff not installed)
- Install: blocked by sandbox firewall

## Next Steps

1. **Fix sandbox firewall + verify tests pass** — add PyPI to allowlist, rebuild image, `pip install -e ".[dev]"`, run `pytest`, `ruff check`, `ruff format --check`. Fix evaluator issues (Python version consistency, Makefile, type hint, conftest cleanup). Small.
2. **Workspace initialization (`assistonauts init`)** — Deliverable #1. TDD: write tests for directory creation, .gitignore, idempotency, then implement. Medium.
3. **Config loading system** — Deliverable #2. TDD: write tests for YAML parsing, defaults, validation, then implement. Medium.
4. **Base agent class** — Deliverable #3. TDD: write tests for toolkit registration, LLM client injection, ownership enforcement. This is the critical-path design decision. Large.

## Session Notes

- The spec document (`docs/assistonauts-spec.md`) received 6 substantive refinements during this session based on architectural review. These changes (phase split, testing strategy, wiki versioning, mission retry, SQLite ceiling, summary quality gap) are now part of the canonical spec.
- The Phase 1a/1b split exists in the spec's prose but REQUIREMENTS.md and PHASE_STATUS.md use a monolithic "Phase 1" with 11 deliverables. The 1a/1b distinction is sequencing guidance within Phase 1, not a hard phase boundary. The natural session ordering follows the deliverable list: scaffolding → workspace init → config → base agent → LLM client → shared toolkit → manifest → Scout.
- The sandbox environment has Python 3.11.2 (not 3.12). All docs need updating to reflect `>=3.11`.
- `uv` is available in the sandbox after install (installed via pip in a background task). Consider using `uv pip install` instead of bare `pip` for speed.
